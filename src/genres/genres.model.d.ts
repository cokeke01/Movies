export interface genreCreationDTO {
  //this waa created for the GenreForm component
  name: string;
}

export interface genreDTO {
  //this waa created for the filterMovies component
  id: number;
  name: string;
}
